---
layout: base.njk
permalink: aktuality.html
canonicalPath: /aktuality.html
title: Aktuality | STAN Praha 13
description: Aktuality Starostů a nezávislých Praha 13.
navKey: news
---
<section class="page-hero">
  <div class="container">
    <h1>Aktuality</h1>
    <p>Akce, místní témata a novinky z naší práce pro Prahu 13. Další články a fotografie budeme postupně doplňovat.</p>
  </div>
</section>

<section class="page-main">
  <div class="container page-panel">
    <div class="section-title"><h2>Nejnovější</h2><p>Sledujte, co chystáme, a přijďte se s námi potkat na Třináctce.</p></div>
    <div class="news-grid">
      {% for item in news | visible %}
      <article class="news-card reveal">
        <span class="news-date">{{ item.dateLabel }}</span>
        <h2>{{ item.title }}</h2>
        <p>{{ item.text }}</p>
        {% if item.buttonUrl %}<a class="button" href="{{ item.buttonUrl }}"{% if item.external %} target="_blank" rel="noopener"{% endif %}>{{ item.buttonLabel }}</a>{% endif %}
      </article>
      {% endfor %}
    </div>
  </div>
</section>
